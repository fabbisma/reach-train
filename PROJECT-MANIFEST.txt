Eco Rail Planner — V0.3.5.2 Global Beta
Clean full-project baseline.

IMPORTANT:
- Canonical search engine: lib/search-engine.ts
- There must NOT be a search-engine.ts at repository root.
- This full build replaces the previous full build.

Expected top-level source folders:
app/
components/
lib/

Expected search engine occurrences: exactly 1
lib/search-engine.ts

V0.3.5.2:
- Correctif Google Places Autocomplete (New): suppression du champ invalide placePrediction.primaryType du field mask.
- Le fallback Transitous est maintenant signalé explicitement dans la liste de suggestions.

- Google Places API (New) autocomplete when a Google key is configured.
- Transitous autocomplete fallback if Places is unavailable.
- Vehicle selector removed from the UI and SearchRequest.
- Generic car profile used only for rough cost/CO2 estimates.
- Existing 60% car-distance and 150% car-time guards preserved.

V0.3.5.2 build safety:
- tsconfig compile scope limited to app/, components/, lib/ and Next type files
- obsolete root-level .ts/.tsx leftovers from old hotfixes can no longer break TypeScript build
